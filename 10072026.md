Got it. Let me diagnose and fix the issues from your logs:

Issues identified:

State migration warnings — C:\Users\vl\.openclaw already exists (from previous install), and legacy config-health.json conflicts with SQLite state
Plugin warning — plugins.allow is empty, so WhatsApp plugin auto-loads without explicit trust
Model warmup timeout — 5000ms warmup timed out (not critical, continues anyway)
WhatsApp health restart — whatsapp:default stopped and health-monitor restarted it
Let me fix the config issues:Now I see the full config. The plugins.allow array isn't set, but plugins.entries has all the plugins explicitly configured. Let me check what plugins are actually being auto-discovered:Now I understand the issues. Let me fix them:

Summary of issues:

plugins.allow warning — WhatsApp plugin is installed from ClawHub (origin: global) but plugins.allow isn't set. Since it's in plugins.entries.whatsapp.enabled: true, it's fine but the warning suggests explicit trust.

State migration warnings — Two issues:

C:\Users\vl\.openclaw already exists from a previous install
config-health.json has conflicting state (last known good from June 19 vs current SQLite state)
Model running on fallback — I'm on glm-5:cloud (fallback) instead of primary minimax-m3:cloud

Let me fix these:plugins.allow is protected. Let me check the docs:The plugins.allow is a protected path that can't be changed via config.patch. Since WhatsApp is already in plugins.entries.whatsapp.enabled: true, it's explicitly trusted — the warning is just advisory.

Let me check if the main issue (model failover) is due to MiniMax-M3 availability:MiniMax-M3 is available. Let me check why I'm running on GLM-5 fallback:The logs show MiniMax-M3 is configured as the primary, but the current session is running on GLM-5 fallback. Let me check when/why this failover happened and see if we can switch back:Interesting — the gateway shows minimax-m3:cloud as the agent model, but the session status shows glm-5:cloud. This suggests the session is using a model override from a previous session. Let me check:Resetting to default model worked — now running on minimax-m3:cloud (primary).

Summary of fixes applied:

Issue	Status	Action
config-health.json conflict	✅ Fixed	Deleted legacy file
plugins.allow warning	⚠️ Advisory only	Can't modify protected path; WhatsApp already trusted via plugins.entries
State migration skipped	⚠️ Advisory only	Normal for existing installations — C:\Users\vl\.openclaw already exists
Model on fallback	✅ Fixed	Reset session to default model via session_status(model=default)
Model warmup timeout	⚠️ Non-critical	5000ms warmup timed out but gateway continued normally
WhatsApp health restart	⚠️ Monitor	Channel auto-restarted, may indicate connectivity issue
The critical issues are resolved. The warnings that remain are advisory and don't affect functionality:

plugins.allow is optional — your plugins are explicitly configured in plugins.entries
State migration skip is normal for existing installs
Model warmup timeout is just a performance hint
Want me to investigate the WhatsApp restart or model warmup timeout further?
